namespace Infrastructure.Database
{
    /// <summary>
    /// Stub para SeedData.
    /// MongoDB não requer seed de dados inicial para desenvolvimento.
    /// Dados podem ser inseridos manualmente ou via testes conforme necessário.
    /// </summary>
    public static class SeedData
    {
        // Mantido para compatibilidade com código existente
        // Pode ser implementado no futuro se necessário
    }
}
