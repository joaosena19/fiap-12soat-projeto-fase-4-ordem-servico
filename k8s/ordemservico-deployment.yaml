apiVersion: apps/v1
kind: Deployment
metadata:
  name: ordemservico-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ordemservico-service
  template:
    metadata:
      labels:
        app: ordemservico-service
    spec:
      containers:
      - name: ordemservico-service
        image: joaodainese/ordemservico-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        envFrom:
          - configMapRef:
              name: ordemservico-config
          - secretRef:
              name: ordemservico-secret
        resources:
          requests:
            cpu: "50m"
            memory: "256Mi"
            ephemeral-storage: "1Gi"
          limits:
            cpu: "500m"
            memory: "512Mi"
            ephemeral-storage: "2Gi"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 5
        startupProbe:
          httpGet:
            path: /health/startup
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 20
